<h1>Listing Emails for all Registered Families for 2009-2010 School Year</h1>

<table>
  <tr>
    <th>Family</th>
    <th>Email</th>
  </tr>
<% i = 0 %>
<% for user in @users %>
	<% student = Student.find_by_userid( user.userid, :order => :firstname) %>

	<% if student != nil && student.registration_year == "2009-2010" %>
	<% i = i + 1 %>
  <tr>
    <td><%=h user.family %></td>
    <td><%=h user.email %></td>
  </tr>
<%end%>
<% end %>
</table>

<br />
<h1> Total number of users: <%= i %></h1>

